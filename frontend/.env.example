# Mapbox Access Token
# Register at https://mapbox.com/ to get a public token (starts with pk.)
VITE_MAPBOX_ACCESS_TOKEN=pk.your_public_token_here

# Backend API URL (if different from default)
# VITE_API_URL=http://localhost:5000/api
