# Python Backend Environment Variables

# Database path (relative to database directory or absolute)
DB_PATH=blood_emergency.db

# JWT Secret (change in production)
JWT_SECRET=your-secret-key

# Server port
PORT=5000
